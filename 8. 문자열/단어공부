ws=input().upper()
u_ws=list(set(ws))

cnt_list=[]
for x in u_ws:
  cnt=ws.count(x)
  cnt_list.append(cnt)

if cnt_list.count(max(cnt_list))>1:
  print('?')
else:
  max_index=cnt_list.index(max(cnt_list))
  print(u_ws[max_index])
