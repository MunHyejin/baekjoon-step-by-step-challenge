n= int(input())

for _ in range(n):
	num=list(map(int,input().split()))
	ave=sum(num[1:]/num[0]
	stu=0
	for score in num[1:]:
		if score>ave:
			stu+=1
	rate = stu/num[0]*100
	print(f’{rate:.3f}%’)
